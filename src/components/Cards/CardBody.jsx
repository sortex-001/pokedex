import { Children } from "react";

const CardBody = (
    {
        children
    }
) => {
    return (
        <div className="cardBody">
            {children}
        </div>
    )
}
export default CardBody;